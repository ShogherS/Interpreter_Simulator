#include <iostream>

int main() {
      int num = 10;
      char ch = 'A';
      bool flag = true; //  ||  1 or 0
      string str = "hello world sunshine<3";
      int i = 0;
      int j = 11;
      while (i < j) {
           std::cout << str[i];
           ++i; // i += 1
      }
      if (i == j) {
          std::cout << “Yes”;
      }
      int arr[5] = {1,2,3,4,5};
      i = 0;
      j  = 5;
      while (i < j) {

	if (i == 4) {
	   break;
}
           std::cout << arr[i];
           ++i; // i += 1
      }
      char array[3];
      i = 0;
      j = 0;
      while (i < j) {
          std::cin >> array[i];
          ++i;
      }
      num += 100;
      num = num + 99;
      if (num > 0) {
	std::cout << num << std::endl;
      }
      return 0;
} 
